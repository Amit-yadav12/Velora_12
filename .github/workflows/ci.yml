# Velora CI — runs on every push and pull request to main.
# Verifies: lint (errors fail), typecheck + production build.
cd Velora_12
git checkout main && git pull
git apply admin-all-business-dashboard.patch
npm ci && npm run build && npm run lint && npm run verify:demo   # expect 74/74
git add -A && git commit -m "Admin console: all-business dashboards; demo admin never stranded in customer app"
git push origin main        # Netlify deploys automatically → then Ctrl+Shift+R
